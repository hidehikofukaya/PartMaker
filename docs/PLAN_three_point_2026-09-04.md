# 3締結点ファミリ（実車007/011の再現）設計 — 2026-09-04

## 0. 目的とスコープ

`fill_mid_surf/A0072600002_AllCATPart` の実車部品 **007** と **011** を、合成側で
再現できる新ファミリ `three_point` を起こす。ユーザー指定の要点は3つ。

1. 締結点の**法線の多様性**を確保する — 「2点の法線が一致、もう1点が異なる」
2. 法線が一致する2点は**ある程度隣接**、3点目の位置関係には**ランダム性**がある
3. 2点と1点の間にある**曲げ線を補強するフランジ**が存在する

本書は実測 → 構造の同定 → 設計 → 判断 → 実装結果、の順。**実装済み**（§7）。

計測ツール: `tools/probe_real_topology.py`、`tools/probe_flange_steep.py`

---

## 1. 実測（中立面STEPの直接計測）

### 1.1 007 — L字ブラケット（面9枚、63×46×41mm、t=2.388）

| 要素 | 面 | 面積mm² | 帯幅(w方向) | 法線/半径 |
|---|---|---|---|---|
| 主パネルA | 7 | 1628 | 33.3 | n=(0,−1,0.01) |
| 主パネルB | 8 | 979 | 29.4 | n=(0,0,−1) |
| **主曲げ** | 0 | 503 | 29.4 | **R10.83**, 軸 = −x |
| フランジ壁a | 4 | 83 | — | 32.9 × **2.7**mm 平板 |
| フランジ壁b | 5 | 133 | — | 51.7 × **3.0**mm 平板 |
| フランジ根本R | 2,3 | 323/501 | — | **R7.33**, 軸 ⊥ w |

- **主曲げは1本のみ**、折れ角 **90.5°**。パネルAとBを繋ぐ。
- フランジは主パネルの**側端**に立つ壁。根本R7.33で75°曲げ、そこから平板が2.7〜3.0mm。
  **総高さ ≈ 9.8mm**。コーナーで2枚に分断（間に面1=bspline、面6=cone の遷移）。

**締結点**

| ID | 種別 | 乗る面 | 帯中心からのw offset | 帯端まで | 最寄り曲げまで |
|---|---|---|---|---|---|
| j0119 | bolt ⌀9.5 | A(7) | +5.3mm | 11.3mm | 11.3mm |
| j0120 | bolt ⌀8.1 | A(7) | −6.6mm | 10.1mm | 23.2mm |
| j0118 | mounting_hole ⌀8.1 | B(8) | −3.7mm | 11.0mm | 18.4mm |

| 組 | 距離 | w方向 | 断面内 | 法線差 |
|---|---|---|---|---|
| j0119–j0120（**対**） | 24.1mm | 11.9 | 21.0 | **0.0°** |
| j0118–j0119 | 43.6mm | +7.1 | 43.0 | 89.5° |
| j0118–j0120 | 59.7mm | −4.8 | 59.5 | 89.5° |

### 1.2 011 — 底板＋立ち壁（面13枚、70×48×29mm、t=1.62）

| 要素 | 面 | 面積mm² | 帯幅(w方向) | 法線/半径 |
|---|---|---|---|---|
| 底パネル | 12 | 1588 | **61.0** | n=(0,0,−1) |
| 立ち壁（大） | 8 | 723 | 43.6 | n=(−0.05,+1,0) |
| 立ち壁（小） | 11 | 293 | 17.4 | n=(−0.06,+1,0) — 8と同一平面 |
| **主曲げ** | 6,7 | 744/297 | 43.6/17.4 | **R10.85**, 軸 = −w |
| フランジ壁 | 1,3 | 132/129 | — | L字 27×23mm、立ち上がりは**3.0mm** |
| フランジ根本R | 0,2,5,9 | 99〜124 | — | **R3.85**, 軸 ⊥ w |

- 主曲げは**1本**（ノッチで6と7に分断）、折れ角 **90.0°**。
- 立ち壁8と11は**同一平面**（ノッチで分断されただけ）。→ 論理的には2パネル構成。
- フランジ壁3は底パネル12（根本0）と立ち壁8（根本5）の**両方**に繋がる
  = **主曲げを跨いで回り込む側端フランジ**。根本R3.85 + 平板3.0 で**総高さ ≈ 6.9mm**。

**締結点**

| ID | 種別 | 乗る面 | 帯中心からのw offset | 帯端まで | 最寄り曲げまで |
|---|---|---|---|---|---|
| j0056 | weld | 立ち壁(11) | +1.1mm | 7.6mm | 2.6mm |
| j0057 | weld | 立ち壁(8) | +7.4mm | 14.4mm | 2.5mm |
| j0123 | bolt ⌀11 | 底(12) | +0.5mm | 30.0mm | 20.5mm |

| 組 | 距離 | w方向 | 断面内 | 法線差 |
|---|---|---|---|---|
| j0056–j0057（**対**） | 36.8mm | **36.8** | **0.2** | **0.5°** |
| j0056–j0123 | 41.2mm | +21.3 | 35.2 | 90.0° |
| j0057–j0123 | 38.5mm | −15.5 | 35.2 | 90.0° |

---

## 2. 構造の同定 — 既存の掃引モデルに収まる

合成側（`occt_build`）は「全ての主曲げ軸が1本の方向 **w** に平行で、部品は w に
直交する平面内の中心線に沿って断面を掃引したもの」しか作れない。007/011 は
**そのまま収まる**。

- 主曲げの軸は 007 が −x、011 が −w の1方向のみ。→ 共通折り軸あり。
- フランジ根本Rの軸は主曲げ軸と直交するが、これは合成側でも同じ
  （フランジは**断面の一部**として掃引されるので、根本Rは自動的に主曲げ軸と直交する）。
  `probe_real_topology.py` の「共通折り軸なし」判定はフランジ根本Rを拾ってしまう
  ための誤判定であり、主曲げだけを見れば両部品とも単一軸。

**したがって新しい幾何エンジンは不要**。既存の `plan_general_two_point` を再利用する。

### 2.1 両部品に共通する骨格

```
      [対の2点]                    [単独の1点]
   同一パネル上                    別パネル上
        |                              |
        +---- 曲げ1本（90°前後） ------+
        |                              |
        +==== 側端フランジが跨いで補強 ==+
```

- 折れは**1回**。2点群と1点の間にちょうど1本。
- 対の2点は同一パネル。法線差 0.0°/0.5°。
- 単独点の法線差は 89.5°/90.0° = 折れ角そのもの。
- 側端フランジが曲げを跨ぐ（011は連続、007はコーナーで分断されるが役割は同じ）。

### 2.2 対の2点の「隣接」の質が2部品で異なる

| | 距離 | w方向成分 | 断面内成分 |
|---|---|---|---|
| 007 | 24.1mm | 11.9 (49%) | 21.0 (87%) |
| 011 | 36.8mm | 36.8 (**100%**) | 0.2 (1%) |

**分離の向きそのものが多様性の軸**。007は主に断面内（掃引方向）、011は完全に
w方向（帯幅方向）。設計ではこの配分を乱数で振る。

---

## 3. 最大の障害 — フランジの折れ角20°上限（解決済み）

`families.flange_part` は `max_fold_angle_deg > FLANGE_MAX_FOLD_ANGLE_DEG (20°)` を
弾く。CATIA時代の申し送り「最大折れ角20度以下ならフランジ、それ以外は
急でフランジ不成立」（2026-08-25）に由来する。

**しかし実車007/011のフランジは90°の曲げに載っている。** ユーザー裁定（2026-09-04）:
「実車が正。前職ではNG寄りだったが、この企業では成立させていた」。

OCCT版の実際の拘束は `occt_build._check_flange_radii` の**凹側クリアランス
`R_bend − h ≥ 2mm` だけ**で、折れ角そのものは効かない。壁は掃引断面の一部なので
曲げ上では「軸に直交する平面内の円環」になり、壁自体は曲がらないためである。

実測（`tools/probe_flange_steep.py`、各帯40試行）:

| 最大折れ角 | 試行 | 成功 | 成功率 | 落ちる理由 |
|---|---|---|---|---|
| 0–20° | 40 | 39 | 98% | — |
| 20–45° | 40 | 37 | 92% | `sample_flange` が側を選べない ×3 |
| 45–70° | 40 | 31 | 78% | 同 ×9 |
| **70–100°（実車帯）** | 40 | 31 | **78%** | 同 ×9 |
| 100–135° | 40 | 32 | 80% | 同 ×7 |

落ちる分は全て「2曲げで両側とも凹になり `sample_flange` が None を返す」ケースで、
**生成失敗（OCCTのエラー）はゼロ**。本ファミリは**1曲げ**なので必ず凸側を選べ、
歩留まりはこれより高くなる見込み。

→ **`FLANGE_MAX_FOLD_ANGLE_DEG` は本ファミリでは適用しない。**
既存 `flange` ファミリの扱いは §6 の未決事項。

---

## 4. 設計

### 4.1 構築手順（既存資産の再利用）

新しい掃引は書かない。次の3段で組む。

1. **骨格**: 対の代表点 P_a と単独点 Q の2点で、既存の
   `plan_general_two_point(..., target_folds=1)` を回す。これで中心線・曲げ1本・
   パネル2枚が決まる。折れ角は実車帯を狙って引く。
2. **フランジ**: 既存 `sample_flange` + `plan_flange_on_surface`。断面の一部として
   掃引されるので、曲げを跨ぐのは自動。20°ゲートのみ外す。
3. **対の相方**: P_a と同じパネル上に P_b を置く。パネル局所座標で
   `(Δs 掃引方向, Δw 帯幅方向)` を振り、下の拘束を満たすまで再抽選。

### 4.2 P_b の拘束（既存ルールを踏襲）

ユーザー指定（2026-09-04）: **締結点の余白と最小Rは従来ルールを踏襲**。

- 帯端まで `≥ min_bearing_radius_mm`（既存の必要平面ルール）
- 曲げの接線まで `≥ min_bearing_radius_mm`（同一パネル内に収める）
- パネルの掃引方向の端まで `≥ min_bearing_radius_mm`
- 中立面R最小 5mm、フランジ根本R 5–8mm は据え置き
  （実車011の根本R3.85は**採用しない** — 我々の下限を優先）
- `check_shape` の「締結点が面上にある」検査に P_b も通す（`MAX_FASTENING_OFFSET_MM = 0.1`）

### 4.3 帯幅の制約と帰結（**要判断**）

現行: `half_width = min(25.0, bearing_radius × U(1.0, 1.3))`、`bearing_radius = U(12.5, 25)`。
つまり帯幅は最大50mm、かつ余白ルールから

```
|Δw| / 2 ≤ half_width − bearing_radius ≤ 0.3 × bearing_radius
→ |Δw| ≤ 0.6 × bearing_radius ≤ 15mm
```

| 実車 | 必要 Δw | 現行で可能か |
|---|---|---|
| 007 | 11.9mm | ✅ 可 |
| 011 | **36.8mm** | ❌ 不可（帯幅61mmが必要） |

余白ルール自体（`half_width ≥ bearing_radius`）は触らない。効いているのは
**帯幅の上限50mm**（`MAX_HALF_WIDTH_MM = 25`）という別ルール。実車011の底パネルは
61mm。→ 本ファミリだけ比率と上限を広げるかが判断点（§6-①）。

### 4.4 パラメータ範囲（実測との対比）

| 項目 | 現行合成 | 実車007 | 実車011 | 本ファミリ案 |
|---|---|---|---|---|
| 折れ回数 | 0/1/2 | 1 | 1 | **1固定** |
| 折れ角 | flange族は4.3–20.1° | 90.5° | 90.0° | **45–120°**（90°付近を厚く） |
| 主曲げR | 10.0–22.5 | 10.83 | 10.85 | 10–18（実車は下端寄り） |
| 帯幅 | 25–50mm | 29–33 | 17–61 | §6-① |
| フランジ根本R | 5–8 | 7.33 | 3.85 | **5–8 据え置き** |
| フランジ総高さ | 10–20mm | 9.8 | 6.9 | §6-② |
| 必要平面R | 12.5–25 | — | — | **12.5–25 据え置き** |
| 対の距離 | — | 24.1 | 36.8 | 20–45mm |
| 対の分離の向き | — | 断面内87% | w方向100% | **0–100%を一様に** |
| 3点目まで | — | 43.6/59.7 | 41.2/38.5 | 既存の締結間距離レンジ |

### 4.5 触るファイル

| ファイル | 変更 |
|---|---|
| `templates/general_two_point.py` | 3点目を持つ `GeneralThreePointSpec`、`target_fold_angle` 指定 |
| `families.py` | `three_point_part()` を追加、`FAMILIES` に登録 |
| `flange.py` | `FLANGE_MAX_FOLD_ANGLE_DEG` を族側の判断へ格下げ（定数は残す） |
| `occt_build.py` | `extra_points` を受け、`check_shape` の面上検査に追加 |
| `annotate.py` | `build_two_joint_pair` → 任意個数の `build_joints` |
| `emit_feature_truth.py` | 3点目とパネル所属をサイドカーに出す |
| `run_occt_batch.py` | `fastener_count` を `{min:2, max:3}` に |
| `tools/recipes/` | `three_point` のレシピ |

### 4.6 検証（生成前に通す）

1. `probe_real_topology.py` を**合成品**に当て、007/011と同じ表が出るか
   （主曲げ1本・法線差0°の対・90°の単独点・曲げを跨ぐフランジ）
2. 歩留まり ≥ 70%（§3の実測78%が下限の目安、1曲げなのでそれ以上）
3. 既存ゲート全部（ゲートA・境界ループ1本・二面角150°・締結点の面上）
4. ビュワーで3点のアノテーションが3枚のパネルに正しく載ること

---

## 5. 決まっていること（ユーザー裁定済み）

- 締結点の穴は開けない（アノテーションのみ）
- 締結点の余白・最小Rは従来ルールを踏襲（2026-09-04）
- 実車が正。急な曲げのフランジは成立させる（2026-09-04）
- 1部品1特徴（フランジのみ。ビード/リブとは併存させない）

---

## 6. 判断結果（2026-09-04 ユーザー裁定）

| # | 論点 | 決定 |
|---|---|---|
| ① | 帯幅の上限 | **本ファミリのみ70mm**（`half_width ≤ 35`）。余白ルールは不変 |
| ② | フランジ総高さ | **現行10〜20mmを据え置き**（実車6.9/9.8mmには寄せない） |
| ③ | 締結点の種別 | **全て `mounting_hole` のまま**（穴は開けないので幾何差なし） |

加えて「締結点の余白と最小Rは従来ルールを踏襲」（`min_bearing_radius_mm` 12.5〜25、
中立面R最小5mm、フランジ根本R 5〜8mm）。

---

## 7. 実装と実測（完了）

### 7.1 追加・変更

| ファイル | 変更 |
|---|---|
| `templates/general_two_point.py` | `GeneralTwoJointSpec.extra_points`。`turn_range_deg` / `half_width_ratio_range` / `max_half_width_mm` / `bend_radius_range_mm` をサンプラーのつまみに |
| `families.py` | `three_point_part()` と `_place_pair_mate()`。`Knobs` に4つのつまみ |
| `occt_build.py` | `extra_points` を受け、`check_shape` の面上検査に通す |
| `annotate.py` | `build_joints()`（任意点数）。`build_two_joint_pair` は薄いラッパとして存置 |
| `batch_generate.py` | `extra_points` の受け渡し。`params.kind` はレシピの族名、特徴は `feature` に |
| `run_occt_batch.py` | `fastener_count` を実測から出す |
| `tools/recipes/occt13.json` | 3点族600部品（本番） |
| `tests/test_three_point.py` | 骨格の不変条件3本 |

**副次的な修正**: 半幅を「比を引いてから上限で切る」形にしていたため、比の上端が
上限を超える族で半幅が上限に張り付いた（実測: 中央値＝上限そのもの）。上限を先に
畳んでから一様に引く形に直した（`_sample_sizes`）。既定のつまみでは従来とほぼ同分布で、
既存4族の回帰も通っている。

**曲げRの切り離し**: 曲げRは既定では半幅から導かれる（`0.9 × half_width` が上限）ため、
帯を70mmに広げるとRまで太った（実測 R19.7〜27.8、実車は10.8）。本ファミリだけ
`THREE_POINT_BEND_RADIUS_RANGE_MM = (8, 18)` で帯から切り離した。

### 7.2 歩留まりと分布（80試行、`random.Random(7)`）

**引けた 80/80、ビルド成功 80/80 = 100%**（2曲げフランジ族の78%より高い。
1曲げなので必ず凸側を選べるため）。生成 0.14〜0.17秒/部品。

| 項目 | 合成（min〜max、中央） | 実車007 | 実車011 |
|---|---|---|---|
| 主曲げR | 8.0〜17.8（13.0） | 10.83 | 10.85 |
| 帯幅 | 29.2〜69.7（53.2） | 29〜33 | 17〜61 |
| 折れ角 | 45.8〜89.7（65.3） | 90.5 | 90.0 |
| 対の距離 | 20.1〜44.1（29.2） | 24.1 | 36.8 |
| 対のΔw（帯幅方向） | 0.0〜17.4（2.7） | 11.9 | **36.8** |
| フランジ高さ | 10.0〜19.7（12.4） | 9.8 | 6.9 |
| フランジ根本R | 5.0〜8.0（6.8） | 7.33 | 3.85 |

### 7.3 トポロジの一致（生成品を `probe_real_topology.py` で再計測）

生成品も007/011と同じ骨格になっている:

```
tp_000: 面7 平面3 曲げ3  主曲げR19.7 折れ角69.4deg  主パネル[0,5]  曲げを跨ぐフランジ壁[3]
tp_001: 面7 平面3 曲げ3  主曲げR27.8 折れ角84.3deg  主パネル[0,5]  曲げを跨ぐフランジ壁[3]
```

主曲げ1本・それに接する主パネル2枚・その曲げを跨ぐ側端フランジ壁1枚。011の
「連続して回り込む側端フランジ」と同型（007はコーナーで2枚に分断される版）。

### 7.4 再現できていない点（既知の限界）

**011型の「対が帯幅方向に36.8mm離れる」配置は作れない。** 到達したΔwは最大17.4mm。

理由は帯幅ではなく**アンカーが帯の中心線に固定されている**こと。掃引の中心線は
`point1`/`point2` そのもので定義されるので、対の一方は必ず帯の中心（w=0）に来る。
従来の余白ルールから相方の可動域は

```
|Δw| ≤ half_width − min_bearing_radius_mm ≤ 35 − 12.5 = 22.5mm
```

実車011のΔw=36.8mmを出すには `half_width ≥ 36.8 + 12.5 = 49.3`（帯幅98.6mm）が必要で、
今回合意した70mmを超える。実車007のΔw=11.9mmはレンジ内で再現できている。

解消するなら、締結点を中心線から外して帯を非対称に張る（`side_extension_mm` の
一般化）改修が要る。フランジが同じ引数を使っているので競合の整理が必要。

---

## 8. 本番生成（occt13、600部品）

`python tools/run_occt_batch.py tools/recipes/occt13.json`（seed 20260920）

- **600部品 / 0.9分（0.07秒/部品）、平均試行 1.0回**（全件が1回目で成立）
- 配置クラス: oblique 489 / orthogonal 111
- 容量: 面 最大7 / エッジ 最大24 / 外形ループ 1

### 8.1 全600件の再検査（生成後に独立実行）

| 検査 | 結果 |
|---|---|
| ゲート不合格 | **0 / 600** |
| ゲートA 最悪プリミティブずれ | **0.000000mm**（上限0.25） |
| 最悪二面角 | **0.0°**（上限150）— 掃引断面なので隣接面が全て接線連続 |
| 外形ループ数 | 全600件が **1本**（崩壊なし） |
| 締結点が面上 | 3点とも全件合格（`MAX_FASTENING_OFFSET_MM = 0.1`） |
| 締結点数 | 600部品すべて **3点**、`fastener_count {min:3, max:3}`、`holes: false` |

### 8.2 構造の一致（60件抜取、折り軸は spec の法線外積から取る）

**60/60 が同一構造**: 主曲げ1本 / フランジ根本R 2枚 / 主パネル2枚 / フランジ壁1枚 /
**その壁が主曲げを跨ぐ 1**。面から再計測した折れ角は 45.0〜88.5°。
実車011の「連続して回り込む側端フランジ」と同型。

> 注: 「面積が最大の円筒＝主曲げ」で判定すると 22/60 で誤判定する。フランジ根本Rは
> 全長にわたって走るので、R が小さくても主曲げより面積が大きくなりうる。主曲げは
> **軸が w に平行な円筒**で選ぶこと（`tools/probe_real_topology.py` の判定も同じ理由で
> 「共通折り軸なし」と誤報する）。

### 8.3 分布（全600件、params から）

| 項目 | 合成（min〜max、中央） | 実車007 | 実車011 |
|---|---|---|---|
| 主曲げR | 8.0〜18.0（13.8） | 10.83 | 10.85 |
| 帯幅 | 27.1〜70.0（55.1） | 29〜33 | 17〜61 |
| 折れ角 | 45.0〜90.0（67.7） | 90.5 | 90.0 |
| 対の距離 | 20.0〜45.0（27.9） | 24.1 | 36.8 |
| 対のΔw | 0.0〜18.7（3.2） | 11.9 | **36.8**（§7.4の限界） |
| フランジ高さ | 10.0〜20.0（13.1） | 9.8 | 6.9 |
| フランジ根本R | 5.0〜8.0（6.5） | 7.33 | 3.85 |
| 対が乗るパネル | point1側 306 / point2側 294 | — | — |
