# 返答: 回答 9 の受領、3 族の床の実測、Q1〜Q7 への回答(2026-09-23)

宛先: PartMaker 側 / 発信: 生成モデル側 / 対象: `REPLY_reseed_floor_2026-09-23.md`(回答 9)

## 0. 受領

450 / 450 本を取り込み(失敗 0)。`outline_distance_u_mean` / `_hausdorff` は manifest から読める。529 の実測(§3.0)は有用で、こちらの依頼文の数字(ピッチ 20〜40、縁から 5〜10)を実測値(30〜70、7〜14)に訂正して受け取る。

## 1. 床の実測(こちらの u = 重心からの RMS 距離 × 2、1mm 再サンプル、両方向平均。角は代替の角 → 元の外形)

| 族 | 床: 外形 中央 / p90 | 床の角(最近傍締結点まで 0〜0.2u / 0.2〜0.4 / 0.4〜0.7 / 0.7〜) | こちらのモデルの角(同じ帯) | 比 |
|---|---|---|---|---|
| occt23 | **0.007 / 0.021** | 0.002 / 0.003 / 0.004 / 0.030 | 0.035 / 0.058 / 0.083 / 0.133 | 19× / 19× / 22× / 4× |
| occt22 | 0.021 / 0.045 | 0.002 / 0.003 / 0.004 / 0.010 | (未測定) | — |
| occt11 | 0.023 / 0.093 | 0.004 / 0.038 / 0.042 / — | 0.016 / 0.065 / 0.057 | 4× / 1.7× / 1.4× |
| occt18(参考、前回) | 0.028 / 0.047 | 0.013 / 0.017 / 0.023 / — | 0.042 / 0.049 / 0.070 | 3× / 3× / 3× |

そちらの `outline_distance_u_mean`(そちらの u)との対応: occt23 0.012 ↔ こちら 0.007、occt22 0.032 ↔ 0.021、occt11 0.023 ↔ 0.023。桁は合っている(Q6 の答え)。

**この測定で決まったこと**: occt23 のモデル誤差(0.066u)は入力の限界ではなく、モデル側の欠陥(床の 19 倍)。分岐(3 倍)と大きく違う。
床はこちらの目的(「同じ入力で許される散らばり」の下限)に十分使える。

## 2. Q1〜Q7 への回答

| # | 回答 | 理由 |
|---|---|---|
| Q1 | **このままで良い**(骨格は固定) | 骨格(ビードの本数、壁の枚数)が違うものは、こちらの学習契約でも「別の設計」(6.34: 構造変種を目標に入れると崩壊)。床は「同じ設計族の中の散らばり」として使う |
| Q2 | **このままで良い** | ビードの位置は外形に表れないので、外形の床には効かない。将来、面段(曲げ・ビード)の床を測るときに改めて依頼する |
| Q3 | **このままで良い** | 帯幅を動かすと点が浮くのは、その部品では帯幅が締結点から決まっているということ。床の定義に合っている |
| Q4 | **「締結点が形状をほぼ決めている」と読む。自由度の追加は不要** | 床は下限として使う。occt22 のモデル誤差 0.18u は床(0.021u)の 9 倍で、自由度を足しても結論は変わらない |
| Q5 | **含めてよい(このまま)** | こちらの学習は `side±1` の変種を等価な目標として扱っている。反転は「同じ入力」の範囲 |
| Q6 | 桁は合っている(§1) | — |
| Q7 | **30 × 5 で足りている** | 中央値は安定。p90 が要るのは占有率の合格条件を書くときで、そのときに occt23 だけ 60 × 5 を頼む可能性がある |

## 3. §3 の相談への回答(判断が要るもの)

- 529 の二重計数: こちらでも確認した。529 の締結点 1,717 のうち 347 点(19 部品)が別の点から 5mm 以内。次のデータ作成で 5mm 以内を 1 点に併合する。指摘に感謝。
- 3.1 溶接列、3.2 パネル族で 20〜60 点、3.3 2 部品の組: **順序は提案どおり(3.1 → 効果測定 → 3.3)で賛成**。

### 3.1 / 3.2 の着手を依頼します(2026-09-24、生成モデル側の責任者が承認)

- **裁定の見直しを承認**: 溶接点は `kind: "spot_weld"` として、座面円盤ではなく**縁距離 d_edge(≥5mm)と曲げ接線からの距離(≥3mm)**で判定する別契約にしてください(2026-09-09「1 部品 1 半径」の例外)。ボルト点は従来どおり。
- **配置**: 実測どおり、縁から 7〜14mm、ピッチ 30〜70mm(部品ごとに乱数)。列の載る辺は §3.1 の候補(occt22 の壁上端・深さ 2 フランジ、occt23 の側辺の壁腕、occt30 の壁腕、掃引のフランジ)。
- **規模と形**: まずは **occt23(パネル)を本命に 20〜40 点の部品を 300**、occt22 で 10〜30 点を 300。新チャンク(`chunk_02` 相当)でお願いします(既存チャンクの変種としてではなく、部品として学習に使うため)。帯長の上限は据え置き(60 点以上は今回不要)。
- **出力に欲しいもの**: 従来と同じ(`joints` に `kind`、`structure`、`seat_ratio_min` は溶接点を除いたボルト点だけで)。溶接点の `d_edge` の実測値を params に入れてもらえると、こちらの縁距離の監査に使えます。
- 3.3(2 部品の組)は、上の効果をこちらで測ってから改めて依頼します。
- モデル側の「20 点まで」は誤りでした。条件行は部品に従って増えるので、60 点でもそのまま使えます(529 の 21〜73 点で確認済み)。
